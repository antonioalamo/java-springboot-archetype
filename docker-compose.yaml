services:
  archetype:
    build:
      context: .
      dockerfile: news.dockerfile
    container_name: news:1.0
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=container
      - ENVIRONMENT=container
    networks:
      - platform

networks:
  platform:
    driver: bridge
